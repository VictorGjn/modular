/**
 * Re-export from @modular/harness (migrated from local copy).
 * @see packages/harness/src/presets.ts
 */
export {
  PRESETS,
  resolvePreset,
  listPresets,
  type AgentPreset,
} from '@modular/harness';
