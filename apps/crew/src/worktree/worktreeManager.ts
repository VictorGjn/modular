/**
 * Re-export from @modular/worktree (migrated from local copy).
 * @see packages/worktree/src/manager.ts
 */
export {
  prepareAgentWorktree,
  getWorktreeStatus,
  rebaseWorktree,
  cleanupWorktrees,
  type WorktreeRequest,
  type WorktreeResult,
  type WorktreeStatus,
} from '@modular/worktree';
